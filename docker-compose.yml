services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        API_BASE_URL: ${API_BASE_URL}
        AUTH_TOKEN: ${AUTH_TOKEN}
    image: doodle-frontend-engineer
    ports:
      - '3001:3001'
    env_file:
      - ./.env
